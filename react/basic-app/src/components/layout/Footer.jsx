export default function Footer({children}){
    return(
            <div>
            <footer style={{backgroundColor: 'cyan'}}>
                {children}
            </footer>
            </div>
    );
}